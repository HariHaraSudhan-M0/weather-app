import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

void main() {
  runApp(MaterialApp(
    home:Scaffold(
      appBar: AppBar(
        title: Text('Weather App',),
        centerTitle: true,
      ),
      body: Center(
        child: Text('hello'),
        ),
    ), 
  ));
}

